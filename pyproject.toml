[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "financial-time-series-lab"
version = "0.1.0"
description = "A reproducible financial time-series research pipeline with ARIMA, GARCH, VaR and LSTM models."
readme = "README.md"
requires-python = ">=3.10"
license = {file = "LICENSE"}
authors = [{name = "Matteo Spata"}]
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "scipy>=1.10",
    "statsmodels>=0.14",
    "matplotlib>=3.7",
    "torch>=2.0",
    "yfinance>=0.2.40",
]

[project.optional-dependencies]
volatility = ["arch>=7.2"]
dev = ["pytest>=7.0", "ruff>=0.4"]
full = ["arch>=7.2", "pytest>=7.0", "ruff>=0.4"]

[project.scripts]
financial-ts = "financial_time_series.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

